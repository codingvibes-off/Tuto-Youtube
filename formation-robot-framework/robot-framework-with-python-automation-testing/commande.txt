python3 -m venv venv
source venv/bin/activate


pip install robotframework robotframework-requests
pip install robotframework-seleniumlibrary

pip install Collections
pip install RequestsLibrary


pip install --upgrade pip

execute script robot   test-api/formation-robot.robot


Validate Invalid Login Shows Error Message
    Open Browser with de mortage payment url
    Fill The Login Form    ${invalid_username}    ${invalid_password}
    Click Button    signInBtn
    Sleep     3s
    wait until checks and display error message
    verify error message is correct


Validate User Can Select User Type
    Open Browser with de mortage payment url
    Fill The Login Form    ${valid_username}    ${valid_password}
    Fill the login Details and select the user option    consult
    Click Button    signInBtn
    Sleep     3s

Validate User Can Select User Type
    Open Browser with de mortage payment url
    Fill The Login Form    ${valid_username}    ${valid_password}
    Fill the login Details and select the user option    teach
    Click Button    signInBtn
    Sleep     3s

Validate User Can Select User Type
    Open Browser with de mortage payment url
    Fill The Login Form    ${valid_username}    ${valid_password}
    Fill the login Details and select the user option    stud
    Click Button    signInBtn
    Sleep     3s
 
Validate Card Titles In Shop Page
    Open Browser with de mortage payment url
    Fill The Login Form    ${valid_username}    ${valid_password}
    Click Button    signInBtn
    Sleep     3s
    verify card titles in the shop page